[package]
name = "music-sync-history"
version.workspace = true
edition.workspace = true

[dependencies]
music-sync-domain = { path = "../domain" }
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
